import 'package:flutter/material.dart';

class OnlineChallengeScreen extends StatelessWidget {
  const OnlineChallengeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}