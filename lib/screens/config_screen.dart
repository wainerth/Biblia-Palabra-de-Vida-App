import 'package:flutter/material.dart';

class Configscreen extends StatelessWidget {
  const Configscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Pantalla de Configuración"));
  }
}