name: biblia_palabra_de_vida_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.5.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  universal_io: ^2.0.4

  provider: ^6.1.2
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_screenutil: ^5.9.3
  google_fonts: ^6.2.1
  smooth_page_indicator:
  shared_preferences: ^2.3.4
  intl: ^0.20.2
  mask_text_input_formatter:
  flutter_svg: ^2.0.10+1
  audioplayers: ^6.1.0
  share_plus: ^11.0.0
  path_provider: ^2.1.5
  image_picker: ^1.1.2
  flutter_sound: ^9.18.0
  permission_handler: ^12.0.0+1
  http: ^1.2.2
  video_player: ^2.9.2
  transparent_image: ^2.0.1
  graphql_flutter: ^5.0.0
  simple_animations: ^5.1.0
  google_sign_in: ^6.2.2
  mime: ^2.0.0
  cached_network_image: ^3.2.3
  flutter_launcher_icons: ^0.14.3 # Usa la versión más reciente
  local_auth: ^2.3.0
  flutter_downloader: ^1.12.0
  open_filex: ^4.7.0
  selectable: ^0.3.1
  flutter_tts: ^3.6.3
  firebase_core: ^3.15.2
  firebase_messaging: ^15.2.10
  socket_io_client: ^3.1.2
  flutter_local_notifications: ^19.3.0
  device_info_plus: ^11.3.0
  flutter_colorpicker: ^1.1.0
  package_info_plus: ^8.3.0
  url_launcher: ^6.3.2
  dio: ^5.4.2  
  http_parser: ^4.1.2
  equatable: ^2.0.7
  timezone: ^0.10.0
  device_preview: ^1.1.0
  js: ^0.6.7
  intl_phone_field: ^3.2.0

  # uso de Stripe

  # para paypal
  webview_flutter: ^4.4.0
  # library for embebed Youtube Video
  youtube_player_flutter: ^9.1.3
  confetti: ^0.8.0



dev_dependencies:
  flutter_test:
    sdk: flutter
  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

flutter_launcher_icons:
  android: true # Generar iconos para Android
  ios: true     # Generar iconos para iOS
  image_path: "assets/icon.png" # Ruta a tu imagen base
  min_sdk_android: 21
# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/
    - assets/barcos/
    - assets/plays/
    # - assets/premios/lñ
    - assets/packages/youtube_player_flutter/assets/speedometer.webp
    - assets/sounds/
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: Erica One
      fonts:
        - asset: fonts/EricaOne.ttf
    - family: Alfa Slab One
      fonts:
        - asset: fonts/AlfaSlabOne.ttf
    - family: Aclonica
      fonts:
        - asset: fonts/Aclonica.ttf
    - family: Alegreya Sans SC
      fonts:
        - asset: fonts/AlegreyaSansSC.ttf
    - family: Ysabeau Infant
      fonts:
        - asset: fonts/YsabeauInfant-Light.ttf
          weight: 300
        - asset: fonts/YsabeauInfant-Regular.ttf
          weight: 400
        - asset: fonts/YsabeauInfant-SemiBold.ttf
          weight: 600
        - asset: fonts/YsabeauInfant-Bold.ttf
          weight: 700
    - family: Roboto
      fonts:
        - asset: fonts/Roboto-Bold.ttf
          weight: 700
        - asset: fonts/Roboto-Light.ttf
          weight: 300
        - asset: fonts/Roboto-Regular.ttf
          weight: 400
    - family: Handwriting  # <--- Este es el nombre que usarás en tu código Flutter
      fonts:
        # Variante regular o base
        - asset: fonts/Handwriting Font 1.ttf  # <--- Ruta al archivo
        
        # Otras variantes (si tienen un peso o estilo específico)
        - asset: fonts/Handwriting Font 3.ttf
          weight: 700 
        
        - asset: fonts/Handwriting Font 5.ttf
          style: italic
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
